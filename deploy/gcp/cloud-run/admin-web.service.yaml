apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: gigabyte-cashback-admin
  annotations:
    run.googleapis.com/ingress: internal-and-cloud-load-balancing
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "5"
    spec:
      containerConcurrency: 40
      timeoutSeconds: 60
      containers:
        - image: asia-east1-docker.pkg.dev/side-project-platform/gigabyte-cashback/admin-web:REPLACE_TAG
          ports:
            - name: http1
              containerPort: 8080
          resources:
            limits:
              cpu: "1"
              memory: 256Mi
